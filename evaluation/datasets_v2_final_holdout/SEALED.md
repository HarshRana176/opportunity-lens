# SEALED FINAL HOLDOUT

12 JDs / 144 candidates. Never inspected by any evaluation or
model-selection run. `load_dataset("evaluation/datasets_v2")` does
not glob this directory; opening it requires an explicit path.

Open EXACTLY ONCE, after a formula has been chosen on dev+validation.
Record the date and reason here when it is opened.

Opened: (never)
